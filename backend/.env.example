# Copie ce fichier en ".env" (non suivi par Git) et remplis les valeurs
# pour développer en local avec l'authentification Supabase activée.

# Supabase → Project Settings → API → Project URL
# (sert à récupérer la clé publique de vérification des jetons, via
# <SUPABASE_URL>/auth/v1/.well-known/jwks.json — aucun secret à stocker ici)
SUPABASE_URL=

# Optionnel en local (sinon SQLite est utilisé par défaut) :
# DATABASE_URL=postgresql://...

# Clé de l'API DecisionBrain (récupération quotidienne des datasets)
DECISIONBRAIN_API_KEY=

# Secret partagé pour protéger la route /api/cron/import-daily.
# Doit être identique ici et dans la variable d'env CRON_SECRET sur Vercel.
# Génère une valeur aléatoire, par ex. avec : python -c "import secrets; print(secrets.token_hex(32))"
CRON_SECRET=
